all:
  children:
    laptops:
      hosts:
        midnight01:
        midnight02:
        m01:
    desktops:
      hosts:
        headquarters0:
    master:
      hosts:
        headquarters0:
    node:
      hosts:
        headquarters0:
    k3s_cluster:
      vars:
        k3s_version: v1.22.3+k3s1
        ansible_user: debian
        systemd_dir: /etc/systemd/system
        master_ip: "{{ ansible_default_ipv4 }}"
        extra_server_args: "--allow-privileged=true"
        extra_agent_args: ""
      children:
        master:
        node:

    
