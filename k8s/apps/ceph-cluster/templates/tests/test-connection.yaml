apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ceph-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "ceph-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ceph-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
